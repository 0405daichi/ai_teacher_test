<% if @all_cards.present? %>
  // @all_cardsにカードが存在する場合のみ更新
  $("#all-cards").html("<%= j(render partial: 'shared/card', collection: @all_cards, as: :card) %>");
<% else %>
  // @all_cardsが空の場合はここに何もしない、またはユーザーに通知するコードを書く
<% end %>
